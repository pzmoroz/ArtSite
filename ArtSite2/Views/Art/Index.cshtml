@model ArtSite2.Model.Picture

@{
    var base64 = Convert.ToBase64String(Model.FileData);
    var imgSrc = String.Format("data:image/gif;base64,{0}", base64);
}

<meta http-equiv="refresh" content="5; URL=http://localhost:57081">

<img class="img-responsive" src="@imgSrc" alt="Chania">